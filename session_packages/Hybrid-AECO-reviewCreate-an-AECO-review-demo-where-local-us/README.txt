Igloo Experience Builder Pilot - Draft session package

This package is a draft session starting point, not a guaranteed production-ready export.
Replace placeholder assets with final media or URLs before using it in a live room.
If the package includes .url files, those are reference links for the content search results the builder selected.